{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && npm install --legacy-peer-deps && CI=false npm run build"
  },
  "deploy": {
    "startCommand": "npx serve -s frontend/dist -l ${PORT:-3000}",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
